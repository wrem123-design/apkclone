# InstaPrime 한글화 작업 안내

## 1. 프로젝트 목적

이 저장소는 InstaPrime 추가 기능 화면을 한국어로 현지화하는 작업용 프로젝트다.

초기에는 앱 실행 후 화면에 보이는 문자열을 별도 로직으로 치환하는 방식도 사용했지만, 현재 기준의 작업 방향은 **코드 자체를 직접 수정해서 한글 문자열이 표시되도록 만드는 방식**이다.

즉, 나중에 이 프로젝트를 처음 보는 사람은 다음 원칙만 기억하면 된다.

- 화면에 보이는 문자열은 가능하면 원본 코드에서 직접 바꾼다.
- 실행 중 임시 번역기처럼 덮어씌우는 방식은 줄이고, 실제 표시 경로에 맞게 수정한다.
- 기능 로직이나 결제/인증/네이티브 동작은 건드리지 않고, UI 문자열만 안전하게 수정한다.

## 2. 핵심 작업 위치

한글화 작업의 핵심 파일은 아래 3개다.

### `apktool_project_smali/smali_classes19/com/primemods/ui/preferences/base/Koreanizer.smali`

가장 중요한 파일이다.

- 설정 화면 제목
- 옵션 이름
- 옵션 설명
- 경고 팝업 문구
- 메인 설정 패널에 보이는 상태 텍스트

위 문자열 대부분이 여기의 `translateString(Ljava/lang/String;)Ljava/lang/String;` 메서드에서 번역된다.

실제 작업은 영어 원문 키를 찾은 뒤, 해당 문자열과 매칭되는 한국어 값을 추가하는 방식으로 진행했다.

### `apktool_project_smali/smali_classes19/com/primemods/ui/preferences/base/BasePreferenceActivity.smali`

이 파일은 예전의 중복 현지화 경로를 정리하기 위해 수정했다.

- 이전에는 `KoreanLocalization.localize(...)` 경로가 별도로 호출되었다.
- 현재는 중복 번역과 충돌을 줄이기 위해 해당 호출을 제거했다.
- 실질적인 번역 적용은 `Koreanizer` 중심으로 맞췄다.

### `apktool_project_smali/smali_classes19/com/primemods/ui/preferences/activity/MainActivity.smali`

메인 설정 진입 화면에서 바로 보이는 문자열을 직접 수정한 파일이다.

- 상단 제목
- 크레딧 문구

같이 눈에 띄는 텍스트를 이 파일에서 직접 한국어로 바꿨다.

## 3. 참고 자료 위치

### `Localizing/`

이 폴더에는 실제 앱 화면을 캡처한 이미지가 들어 있다.

역할은 다음과 같다.

- 번역이 덜 된 화면 확인
- 실제 사용자에게 보이는 영문 문구 확인
- OCR로 문자열 후보 추출
- 번역 적용 후 검수 기준으로 사용

이번 작업에서는 이 폴더의 이미지 12장을 기준으로 남아 있던 영문을 찾아서 반영했다.

## 4. 이번에 반영한 내용

이번 한글화 작업에서는 아래 영역을 집중적으로 정리했다.

- 개발자 옵션 화면
- 개발자 경고 팝업
- UI 조정 화면
- 다운로드 화면
- 피드 및 스토리 화면
- 제스처 화면
- 고스트 모드 화면
- 알림 필터 화면
- 개인정보 화면
- 메인 설정 패널 상태 텍스트

구체적으로는 다음과 같은 종류의 문자열을 직접 반영했다.

- 섹션 헤더
- 토글 옵션 제목
- 옵션 설명문
- 상태 텍스트
- 경고/안내 문구
- 줄바꿈이 포함된 멀티라인 문구

또한 실제 화면에서는 같은 문장이 줄바꿈 여부나 특수문자 차이로 약간 다르게 들어오는 경우가 있어서, 동일 의미의 변형 문자열도 함께 추가해 누락 가능성을 줄였다.

## 5. 작업 방식

실제 작업 순서는 아래와 같다.

1. `Localizing/` 이미지에서 미번역 화면 확인
2. OCR과 직접 육안 확인으로 영어 원문 추출
3. `Koreanizer.smali`의 `translateString(...)`에 원문/번역 쌍 추가
4. 필요한 경우 `MainActivity.smali`와 `BasePreferenceActivity.smali` 보정
5. `apktool b`로 재빌드
6. APK 서명 후 기기 설치
7. 실제 기기 화면 기준 재검수

## 6. 빌드 및 설치 메모

로컬 빌드 산출물은 작업 중 `build_apk/` 폴더에서 만들었다.

일반 흐름은 아래와 같다.

- `apktool b apktool_project_smali -o build_apk/...`
- `zipalign`
- `apksigner`
- `adb install -r`

최종 검사용 APK는 루트에 별도 이름으로 보관했다.

## 7. 유지보수 시 주의사항

이 프로젝트를 나중에 이어서 작업할 사람이 주의할 점은 다음과 같다.

- 문자열 수정의 우선 위치는 `Koreanizer.smali`다.
- 이미 한글화된 화면이라도 줄바꿈 차이 때문에 다른 원문 키가 있을 수 있다.
- 네이티브 라이브러리 `libprimemods.so` 쪽 로직은 가능하면 건드리지 않는다.
- 기능이 바뀌는 수정은 피하고, 표시 문자열만 바꾸는 쪽을 우선한다.
- 새 미번역 화면이 보이면 먼저 `Localizing/`에 캡처를 추가한 뒤 작업하면 추적이 쉽다.

## 8. 현재 상태

현재 기준으로는 `Localizing/`에 들어 있는 이미지 범위에서는 한글화가 완료된 상태로 판단했다.

다만 앱 전체 100%를 완전히 단정하려면, 이미지에 없는 숨은 화면이나 새로 열리는 하위 메뉴까지 실제 기기에서 추가 검수가 필요할 수 있다.

그래도 이 저장소 기준의 한글화 작업 위치와 수정 흐름은 이 문서만 보면 바로 파악할 수 있도록 정리했다.
