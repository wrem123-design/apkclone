/**
 * (c) Meta Platforms, Inc. and affiliates. Confidential and proprietary.
 */

function(isEmployee) {
  if (window._ActivateReaderModeMultiWindow) {
    window._ActivateReaderModeMultiWindow(isEmployee);
  }
}
